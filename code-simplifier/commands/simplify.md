---
description: 调用 simplify 子智能体精简最近改动的代码（默认未提交改动，可指定 commit、分支或文件路径）
argument-hint: "[commit|分支名|文件路径]"
---

把下面的代码精简任务派发给 `simplify` 子智能体执行，不要自己直接精简。

精简目标（原样传给子智能体；为空表示处理全部未提交改动）：

$ARGUMENTS

执行要求：

- 通过 Agent 工具派发，subagent_type 为 `code-simplifier:simplify`（插件命名空间形式；若工具可用类型列表中显示为其他名称，如裸名 `simplify`，以列表中的实际名称为准），前台执行并等待其完成。
- 将上面「精简目标」下的内容（可能为空）作为任务提示传给子智能体，不要改写或补充。
- 子智能体完成后，把它返回的改动总结完整、忠实地转述给用户：保留每处改动的位置与理由，以及它列出的发现的 bug，不要遗漏、合并或简化，也不要追加你自己的额外评论。
